services:
  - type: web
    name: islamic-will-app
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: islamic-will-db
          property: connectionString

databases:
  - name: islamic-will-db
    plan: free
    databaseName: islamic_will
